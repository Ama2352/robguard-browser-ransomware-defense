Place the following files here after running Stage 8 of the Google Colab notebook:

  best_model.pkl       — the model with highest Recall (determined by Stage 7)
  scaler.pkl           — StandardScaler fitted on the training data

These files are excluded from git (.gitignore). Regenerate them by running
notebooks/ransomware_detection.ipynb and downloading the output.
